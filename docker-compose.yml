services:
  postgres:
    image: postgres:17
    container_name: postgres
    environment:
      POSTGRES_DB: KPOStudyTrackerDB
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build: .
    container_name: KPOStudyTrackerAPP
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql+asyncpg://postgres:postgres@postgres:5432/KPOStudyTrackerDB
    ports:
      - "8000:8000"

volumes:
  postgres_data: